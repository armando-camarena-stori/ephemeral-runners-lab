name: Self-hosted runner test

on:
  push:
  workflow_dispatch:

jobs:
  test-on-self-hosted:
    runs-on: [self-hosted, macOS, lab]

    steps:
      - name: Say hello from self-hosted
        run: |
          echo "Hola desde mi Mac 🚀"
          sw_vers

